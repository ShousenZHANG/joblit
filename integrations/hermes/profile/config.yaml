model:
  provider: openai-codex
  openai_runtime: auto

platform_toolsets:
  api_server:
    - no_mcp
  cron:
    - no_mcp

memory:
  memory_enabled: false
  user_profile_enabled: false

honcho:
  enabled: false

agent:
  disabled_toolsets:
    - all
